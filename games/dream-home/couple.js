/* dream-home couple module — STUB (parallel build session). Same contract as court.js.
   Goal: deepen koto & zuza's bond — shared actions, gifts, affection meter. */
(function () {
  const DH = (window.DH = window.DH || {});
  DH.couple = {
    init() {}, start() {}, update() {},
    interactables() { return []; },
    collectDraws() {}, drawGround() {}, drawOverlay() {},
    serialize() { return {}; }, deserialize() {}, remoteAction() {},
  };
})();
